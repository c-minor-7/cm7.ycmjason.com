{
  "name": "cm7.ycmjason.com",
  "version": "0.2.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preinstall": "npm --prefix functions i",
    "preversion": "npm run build && $EDITOR CHANGELOG.md",
    "version": "git add -A",
    "predeploy": "git checkout $(git describe --tags)",
    "deploy": "firebase deploy",
    "postdeploy": "git checkout -",
    "dev": "nuxt",
    "serve": "firebase serve",
    "prebuild": "npm run clean",
    "build": "nuxt build",
    "postbuild": "./scripts/prepareCDN.js && ./scripts/prepareSSR.js",
    "build:favicon": "faviator -c favicon.config.json -o nuxt-src/static/favicon.png",
    "lint": "eslint . --fix",
    "clean": "/bin/rm -rf functions/ssr/nuxt-dist public-dist nuxt-dist",
    "clean:modules": "/bin/rm -rf functions/node_modules node_modules"
  },
  "keywords": [],
  "author": "Jason Yu <me@ycmjason.com>",
  "license": "ISC",
  "dependencies": {
    "@nuxtjs/google-analytics": "^2.0.2",
    "cm7-vue": "^0.1.0",
    "deepmerge": "^2.2.1",
    "normalize.css": "^8.0.0",
    "nuxt": "^2.1.0"
  },
  "devDependencies": {
    "@ycm.jason/eslint-config": "^1.3.1",
    "eslint": "^5.6.1",
    "eslint-plugin-vue": "^4.7.1",
    "faviator": "^1.2.0",
    "fs-extra": "^7.0.0",
    "markdown-loader": "^4.0.0",
    "node-sass": "^4.9.3",
    "sass-loader": "^7.1.0"
  }
}
